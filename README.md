# 🚀 نظام إدارة المدونات

مشروع إدارة مدونات مبني على إطار عمل لارافيل ، يهدف إلى توفير واجهة كاملة لإنشاء، تحرير، ونشر المدونات، مع دعم كامل للفئات  ومستخدمين متعددين (Admin/User).

## 🌟 الميزات الرئيسية

* **إدارة المحتوى:**    , إنشاء، تعديل، وحذف المدونات.,وحذف ناعم , واسترجاع
* **تصنيف مرن:** دعم علاقة Many-to-Many بين المدونات والفئات.
* **نظام المستخدمين:** أدوار محددة (`admin` و `user`).
* **تخزين آمن:** حفظ الصور في Storage محمي (`storage/app/private/uploads`).

---

## 🛠️ المتطلبات الأساسية

تأكد من تثبيت الأدوات والتقنيات التالية على جهازك:

* **PHP:** الإصدار 8.1 أو أحدث.
* **Composer:** أداة إدارة حزم PHP.
* **قاعدة البيانات:** MySQL / MariaDB.

---

## 💻 خطوات التثبيت والتشغيل

اتبع هذه الخطوات لتجهيز المشروع وتشغيله محلياً.

###  استنساخ المستودع (Clone Repository)

```bash
```git clone https://github.com/aliyounes606/Blog-management-system.git```
```cd Blog-management-system```

ثم قم بتثبيت حزم PHP المطلوبة:

```composer install```


انسخ ملف .env.example إلى .env.

```cp .env.example .env```


قم بتوليد مفتاح التطبيق:

```php artisan key:generate```

تعديل اتصال قاعدة البيانات في ملف .env (تأكد من إنشاء قاعدة بيانات فارغة أولاً):

DB_CONNECTION=mysql
DB_DATABASE=blogs_db
DB_USERNAME=root
DB_PASSWORD= 


تشغيل المهاجرات وتعبئة البيانات (Seeding)

```php artisan migrate:fresh --seed```

لمستخدم  	البريد الإلكتروني   	كلمة السر	الدور
ali  	    ali@ali.com	            00000000	  admin
issa  	   issa@issa.com	        00000000	  user


بناء الأصول الأمامية (Frontend Assets)
لتجميع ملفات CSS و JavaScript:

```npm install```
```npm run dev```

إنشاء الرابط الرمزي (Storage Link)
لربط مجلد التخزين بالمسار العام:

```php artisan storage:link```

تشغيل الخادم المحلي

```php artisan serve```

يمكنك الآن الوصول إلى التطبيق عبر الرابط: http://127.0.0.1:8000